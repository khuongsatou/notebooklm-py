# Production renderer configuration.
# Copy to .env.production when building a browser-hosted production renderer.

VITE_NOTEBOOKLM_ENV=production

# Leave blank for Electron production builds so requests go through the preload
# bridge. Set to a public REST origin only for a browser-hosted deployment.
VITE_NOTEBOOKLM_API_BASE=
